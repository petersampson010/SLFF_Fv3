import { vw, vh } from "react-native-expo-viewport-units";
import { $seaBlue } from "../../styles/global";

export const gwScoreContainer = {
    width: vw(96),
    marginHorizontal: vw(2),
    flexDirection: 'row',
    justifyContent: 'center',
    marginTop: vh(1),
}