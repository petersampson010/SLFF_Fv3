import { vh, vw } from "react-native-expo-viewport-units"
import { $darkBlue, $spaceCadet } from "../../styles/global"


export const pitchContainer = {
    // height: vh(350)
}

export const banner = {
    width: '100%',
    flexDirection: 'row',
    justifyContent: 'center'
}

export const bannerText = {
    color: $darkBlue,
    padding: vw(1),
    fontWeight: '700'
}