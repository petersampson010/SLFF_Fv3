import { vh, vw } from "react-native-expo-viewport-units";
import { $baseBlue, $chocolateBlack, $coral, $onyx, $platinum, $sage, $skobeloff } from "../../styles/global";

export const header = {
    width: vw(100),
    height: vh(5),
    backgroundColor: $platinum,
}

export const headerText = {
    textAlign: 'center',
    fontSize: 22,
    color: $skobeloff
}